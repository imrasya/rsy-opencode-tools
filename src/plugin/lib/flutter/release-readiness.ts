import type { FlutterProjectScan } from "./project-scanner.js";
export interface FlutterReleaseReadiness { ready: boolean; checks: { status: "pass" | "warning" | "fail"; name: string; message: string }[]; commands: string[] }
export function assessFlutterReleaseReadiness(scan: FlutterProjectScan): FlutterReleaseReadiness { const checks: FlutterReleaseReadiness["checks"] = []; checks.push(scan.detected ? { status: "pass", name: "flutter-project", message: `Flutter project ${scan.name ?? "unknown"} detected.` } : { status: "fail", name: "flutter-project", message: "Flutter project not detected." }); checks.push(scan.version ? { status: "pass", name: "version", message: `pubspec version ${scan.version} detected.` } : { status: "fail", name: "version", message: "pubspec version is missing." }); if (scan.platforms.includes("android")) checks.push({ status: "warning", name: "android-signing", message: "Verify Android signing credentials and Play policy outside static scan." }); if (scan.platforms.includes("ios")) checks.push({ status: "warning", name: "ios-signing", message: "Verify iOS bundle id, provisioning, and privacy manifests on macOS/Xcode." }); const commands = [scan.platforms.includes("android") ? "flutter build appbundle" : undefined, scan.platforms.includes("web") ? "flutter build web" : undefined].filter(Boolean) as string[]; return { ready: checks.every((check) => check.status !== "fail"), checks, commands }; }
